def solution(num):
    # 초기 작업 횟수를 0으로 설정
    answer = 0

    # 주어진 수가 1이 될 때까지 반복
    while num != 1:
        # 주어진 수가 짝수일 경우, 2로 나눔
        if num % 2 == 0:
            num /= 2
        else: 
            # 주어진 수가 홀수일 경우, 3을 곱하고 +1
            num = num * 3 + 1
        
        # 작업을 한 번 수행할 때마다 카운트 증가
        answer += 1
        
        # 작업 횟수가 이미 지정된 최대치(500번)에 도달한 경우, -1 반환 후 종료.
        if answer == 500:
            return -1
    
    # 반복문이 종료되면 (즉, 주어진 수가 결국에는 '1'이 되면), 현재까지의 작업 횟수를 반환.
    return answer
