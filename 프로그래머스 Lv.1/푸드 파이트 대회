def solution(food):
    # 빈 문자열로 초기화된 answer 변수를 선언합니다. 
    # 이 변수는 나중에 각 선수가 먹을 음식의 순서를 나타내는 문자열로 사용됩니다.
    answer = ''

    # food 리스트의 모든 요소에 대해 반복합니다. 
    # 여기서 i는 음식의 인덱스(또는 번호)를 나타냅니다.
    for i in range(len(food)):
        # 각 음식 번호(i)를 문자열로 변환하고, 해당 음식을 두 선수가 공평하게 나누어 먹을 수 있는 양만큼 반복합니다(food[i]//2).
        # 이 문자열은 answer에 추가됩니다.
        answer += str(i)*(food[i]//2)

    # 모든 음식이 배치된 후, 중앙에 위치할 물(음식 번호 0)을 추가합니다.
    return answer + "0" + answer[::-1]  
  	# 기존의 음식 배치(answer)와 그것의 역순(answer[::-1]) 사이에 물("0")이 오도록 최종적인 음식 배치를 반환합니다.
