def solution(s):
   answer = 0  # 분해된 문자열의 개수를 저장할 변수
   cnt1=0; cnt2=0  # 첫 글자와 그 외의 글자들이 나온 횟수를 저장할 변수

   for i in s:  # 문자열 s의 각 문자에 대해
       if cnt1==cnt2:  # 첫 글자와 그 외의 글자들이 나온 횟수가 같으면
           answer+=1  # 분해된 문자열 개수 증가
           k=i  # 첫 글자를 현재 문자로 설정

       if k==i:  # 현재 문자가 첫 글자와 같으면 
           cnt1+=1  # 첫 글자 등장 횟수 증가
       else:  
           cnt2+=1  # 그 외의 다른 글자 등장 횟수 증가

   return answer  
