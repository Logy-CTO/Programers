def solution(n):
    answer = 0
    
    # x를 1부터 n-1까지 순회합니다.
    for i in range(1, n):
        # n을 x로 나눈 나머지가 1이면
        if n % i == 1:
            # 해당 x 값을 정답으로 설정하고 반복문을 종료합니다.
            answer = i
            break
    
    return answer
